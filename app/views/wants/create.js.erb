<% if @want.id.nil? %>
  // Render new review form with errors
  $record = $('#wantlist .record[data-record-id="<%= @want.record.id %>"] .record-infos');
  text = $record.html();
  $record.empty().html("<h2>Only 10 digs at the same time</h2>");
  setTimeout(function() {
    $record.html(text);
  }, 900);
<% else %>
  // $record = $('.indic[data-record-id="<%= @want.record.id %>"]');
  // $record.html('<svg class="circular" viewBox="25 25 50 50"><circle class="path" cx="50" cy="50" r="20" fill="none" stroke-width="2" stroke-miterlimit="10"></circle></svg>').attr('class', 'indic indic-wanted');
  

  // $record = $('#wantlist .record[data-record-id="<%= @want.record.id %>"]');
  // $clone = $record.clone().addClass('clone').empty().html("<h2 class='added'>Added to Diglist</h2>").appendTo('#wantlist');
  // $clone.css({
  //   'z-index': 500,
  //   'position': 'fixed',
  //   'background': '#fff',
  //   'width': "30vw",
  //   'top': $record.offset().top,
  //   'left': $record.position().left,
  //   'margin': 0
  // });
  // $record.css('opacity', '0');
  // setTimeout(function() {
  //   $clone.remove();
  //   $record.remove();
  // }, 900);

  $record = $('#wantlist .record[data-record-id="<%= @want.record.id %>"]');
  $record.addClass('digged').find(".record-infos").empty().html("<h2>Added to the Diglist</h2>");
  $('#dig-counter').text("<%= @wants.count %> digs");
  $('#wants-counter').html("<%= escape_javascript(render('shared/wants_counter', wants: @wants)) %>");
  $('.status.title').html('<span class="wanted">ging<span>.</span><span>.</span><span>.</span></span>');

  setTimeout(function() {
    $record.addClass('wanted');
    $('#wants .record[data-record-id="<%= @want.record.id %>"]').addClass('wanted');
  }, 900);
<% end %>