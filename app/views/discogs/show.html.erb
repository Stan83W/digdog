<%= render template: 'discogs/wantlist' %>

<div id="page-content">
  
<div class="status title">
  <span class="wanted">
    ging<span>.</span><span>.</span><span>.</span>
  </span>
  <span class="found">
    found <%=  %>
  </span>
</div>
      
<div class="c12 x">
  <div class="record-cover c4">
  <%= image_tag @record.images.first["uri"], class: "w100" %>
  </div>
  
  <div class="c8 px2">
    <h1 class="record-title"><%= @record.title %></h1>
    <% @record.artists.first(1).each do |artist| %>
      <h2><%= artist["name"] %></h2>
    <% end %>
  </div>
</div>

<div class="c12 x">
  <div class="c4">
      <% @record.labels.each do |label| %>
        <h2>Labels: <%= label["name"] %></h2>  
        <% end %>
        
      <% unless @record.formats.nil?  %>
        
        <% @record.formats.each do |format| %>
          <h2>Format: <%= format["name"] %></h2>
        <% end %>        
      <% end %>
      <h2>Year: <%= @record.year %></h2>
        
        
        <% @record.genres.each do |genre| %>
          <h2>Genres: <%= genre %></h2> 
        <% end %>
   
        <% @record.styles.each do |style| %>
          <h2>Styles: <%= style %></h2> 
        <% end %>
    
  </div>
    <div class="c8 px2">
        
        
        
        <% unless @record.tracklist.nil?  %>
            <h2 class="subtitle">Tracklist</h2>
            <div class="c12 subtitle">
                <% @record.tracklist.each do |tracklist| %>
                <div class="track c12 x">
                  <div class="c1">
                    <%= tracklist["position"] %>        
                  </div>
                  <div class="c10">
                    <%= tracklist["title"] %>     
                  </div>
                  <div class="c1">
                    <%= tracklist["duration"] %>
                  </div>
                </div>
              <% end %>
            </div>
        <% end %>
      </div>
    </div>
  
</div>