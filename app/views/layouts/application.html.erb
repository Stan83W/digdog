<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Digdog helps you find rare vinyls without the hassle of entering the same search into every website day after day.">
    <link rel="stylesheet" href="https://unpkg.com/flickity@2/dist/flickity.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/octicons/3.5.0/octicons.css">
    <title>Digdog</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>
  <body<%= ' class=page-content' if !@record.nil? %>>
    <%= render "shared/header" %>
    <div class="wrapper">
      <%= render 'shared/flashes' %>
      <div id="close-panel" data-target="close-panel"></div>
      <% if @record.nil? %>
        <div id="page-content"></div>
      <% end %>
      <%= yield %>
      <%= javascript_include_tag 'application' %>
    </div>
  </body>
</html>
